version: '3.7'
services:
    app:
        container_name: desafio-aws-amcom-app
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - '.:/app'
            - '/app/node_modules'
        ports:
            - '3001:3000'
        environment:
            - NODE_ENV=development
            - REACT_APP_USERPOOL_ID=
            - REACT_APP_CLIENT_ID=
